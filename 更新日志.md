# 更新日志

## 2026-05-11

### 第二大脑展示网站重构

- 根据《第二大脑构建系统：全息实操百科全书》重新梳理展示站信息架构。
- 将原本集中在 `src/App.jsx` 的大体量页面拆分为应用壳、主题、章节渲染、可视化组件和内容数据。
- 新增 `src/data/knowledgeBase.js`，集中维护导航、模块文案、PARA 清单、系统流转和目录结构。
- 保留侧边导航、搜索、深浅色切换和互动可视化展示体验。
- 修复原页面中的中文乱码标题，并新增项目 favicon。
- 移除 Vite 模板残留的 `src/App.css` 和 `src/assets/react.svg`。

### 验证

- `node node_modules/eslint/bin/eslint.js .`
- `node node_modules/vite/bin/vite.js build`
- 使用本地 Chrome headless 模式确认页面能正常渲染。
